export interface UserformsattachmentsInterface {
    id: number;
    user_form_id: number;
    attachments: string;
    status: string;
    created: string; // or `Date` if parsed
    updated: string; // or `Date` if parsed
}
